name: Custom Checkout
description: Perform custom Git checkouts in Buildkite pipelines
author: https://github.com/saah13
requirements:
  - git
configuration:
  properties:
    repository:
      type: string
    branch:
      type: string
    commit:
      type: string
    path:
      type: string
  additionalProperties: false
