name: Custom Checkout
description: Specify a custom Git repository, branch, or commit to checkout
author: https://github.com/buildkite-plugins
requirements:
  - git
configuration:
  properties:
    repository:
      type: string
    branch:
      type: string
    commit:
      type: string
  required:
    - repository
